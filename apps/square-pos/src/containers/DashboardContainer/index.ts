// barrel file that re-exports modules from the directory
// Instead of importing from many different files, you import from the directory

export  { default as DashboardContainer } from "./DashboardContainer";
